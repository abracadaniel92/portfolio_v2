# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Built from blog/*.md by scripts/build-blog.mjs on every dev/build/lint run.
# The markdown is the source; this is derived output.
src/blog/posts.generated.ts

# Local Claude Code config. Skills ARE tracked on purpose so they travel with the
# repo to other machines; everything else under .claude/ is machine-specific
# (launch.json holds absolute local paths) and stays local.
.claude/*
!.claude/skills/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
